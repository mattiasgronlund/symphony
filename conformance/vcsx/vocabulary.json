{
  "artifact": "vcsx-token-vocabulary",
  "schema_version": 2,
  "governed_by": "VCSX-SPEC.md",
  "description": "The shared token vocabulary VCSX-SPEC.md Section 14 requires to be spelled identically in VCSX-SPEC.md, VCSX-CONTRACT.md, and any engine implementation. A derived view: the specification governs, this file restates no requirement's substance, and a disagreement is a bug here.",
  "spec_refs": [
    "VCSX-SPEC.md Section 14 \"Alignment with `VCSX-CONTRACT.md`\""
  ],

  "proto_classes": {
    "spec_refs": ["VCSX-SPEC.md Section 4.2 \"Operation Result Envelope and Proto Classes\""],
    "note": "Part of the major-stable surface (Section 8.5). A listed reason's class MUST NOT change within a MAJOR.",
    "entries": [
      { "token": "done", "meaning": "The operation reached its intended effect, including a benign no-op." },
      { "token": "needs_caller", "meaning": "The operation cannot proceed without a decision or action from the caller; not an engine failure." },
      { "token": "error", "meaning": "The operation failed." }
    ]
  },

  "operations": {
    "spec_refs": ["VCSX-SPEC.md Section 4.1 \"Operation Set\""],
    "note": "The set VCSX-SPEC.md fixes, extended only by a MINOR release and never by an individual engine (Sections 4.1, 8.5). A trigger token outside the running version's set is `unknown_trigger` on every conforming engine rather than on some of them (Section 6.11). `read_only` carries Section 4.1's marker — the operation changes none of the content a `commit` would capture, the commits reachable from the work branch or the resolved base, and what the remote holds — and `lifecycle_position` the `before:<op>` position the operation is gated at, or null where it carries none. `policy_dispatchable` carries Section 4.1's other marker: false for an operation that runs outside the action-policy machine — no `run_op` edge may name it (`operation_not_dispatchable`, Section 6.11) and it raises no `<op>:<reason>` trigger an `on` may name (Section 5.1) — and true for the rest. It is fixed by the property rather than by the two names it reaches today, so an operation a MINOR release adds with that property inherits both refusals; it is deliberately not derivable from `reasons`, since `provision` runs outside the machine and carries three class-bearing reasons.",
    "entries": [
      { "token": "load_policy", "read_only": true, "lifecycle_position": null, "policy_dispatchable": false, "note": "Obtains the merged host-side policy surface once for a unit of work, returning it for the consumer to inspect together with the `policy_pin` naming it; every invocation reads and validates the document itself, and one supplying the pin is refused where what it validated is not the surface the pin names (Sections 8.1, 8.2, 8.6). Like `provision` it has no lifecycle position, raises no `<op>:<reason>` trigger and may be named by no `run_op` edge, the edges that would gate, route or dispatch it being in the document it exists to obtain. Its failures are the four Section 6.1 conditions, reported as configuration errors, so `config_reasons` carries them and `reasons` has no `load_policy` entry (Sections 4.1, 6.1, 6.11)." },
      { "token": "status", "read_only": true, "lifecycle_position": null, "policy_dispatchable": true },
      { "token": "diff", "read_only": true, "lifecycle_position": null, "policy_dispatchable": true },
      { "token": "provision", "read_only": false, "lifecycle_position": null, "policy_dispatchable": false, "note": "Runs outside the action-policy machine, as `load_policy` does: an edge dispatching it was read out of the repository the operation obtains, so it could fire only where the repository was already there — a trigger that sometimes exists (Sections 4.1, 5.4). It carries reasons and a proto class all the same, which is why `policy_dispatchable` is a marker of its own rather than a property read off `reasons`." },
      { "token": "commit", "read_only": false, "lifecycle_position": "before:commit", "policy_dispatchable": true },
      { "token": "integrate", "read_only": false, "lifecycle_position": null, "policy_dispatchable": true },
      { "token": "push", "read_only": false, "lifecycle_position": "before:push", "policy_dispatchable": true },
      { "token": "create_pr", "read_only": false, "lifecycle_position": "before:create_pr", "policy_dispatchable": true },
      { "token": "merge", "read_only": false, "lifecycle_position": "before:merge", "policy_dispatchable": true },
      { "token": "pull", "read_only": false, "lifecycle_position": null, "policy_dispatchable": true },
      { "token": "await_checks", "read_only": true, "lifecycle_position": null, "policy_dispatchable": true, "note": "Gated at no position for a reason of its own: a gate before a wait would run a unit that inspects nothing and blocks nothing worth blocking. Counts once against the flow bound however many reads it makes (Section 5.6). Only `await_bound_ms` and `await_max_reads` authorize a second read; `await_interval_ms` paces reads an allowance already authorized and `await_budget_floor` can only end a wait early, so an invocation supplying one of the latter and neither of the former is refused with `await_bound_missing` (Sections 8.1, 8.6)." }
    ]
  },

  "lifecycle_positions": {
    "spec_refs": ["VCSX-SPEC.md Sections 4.1 \"Operation Set\", 5.1 \"Triggers\""],
    "note": "The positions VCSX-SPEC.md fixes, extended only by a MINOR release and never by an individual engine (Sections 4.1, 8.5). Matched exactly; a lifecycle position has no #class form (Section 5.3). `load_policy` and `provision` have none and raise no `<op>:<reason>` trigger: both are matched against `[policy]` edges read from `repo.policy.toml`, which is the document `load_policy` obtains and is inside the repository `provision` obtains, so a gate on either would be absent on the invocation that produces what the edge was read out of and present on one that finds it already there (Section 4.1). The same sentence refuses a `run_op` edge naming either, which `operations` carries as `policy_dispatchable: false`.",
    "entries": ["before:commit", "before:push", "before:create_pr", "before:merge"]
  },

  "reasons": {
    "spec_refs": ["VCSX-SPEC.md Section 4.3 \"Reason-Token Registry\""],
    "note": "The Section 4.3 table normalized to one entry per (operation, reason). Its combined rows expand: the `status` / `diff` row to two entries, and the four universal rows to one entry per operation they cover — `failed` and `unsupported` for every operation this registry covers, `blocked` and `hook_unanswered` for every gated one — each marked `universal: true`. What the registry covers is the invariant Section 4.3 states rather than a list: an operation whose every outcome the specification reports as a configuration error carries no reason here and no universal reason reaches it, which is `load_policy` today and is why this group has no entry for it. `provision` is covered, its result not re-entering the machine being a fact about routing rather than about reporting. `blocked`, `failed` and `hook_unanswered` divide a gate's outcomes by what the hook did: refused with a `needs_caller` result, refused with an `error` result, and gave no usable answer at all. An engine MUST document any reason it adds beyond this set, with its class. Every entry carries `default_need`: for a `needs_caller` reason, the need an escalation on that result carries where nothing in the policy named one (Section 4.3's column); `null` for a `done` or `error` reason, whose class default continues or fails rather than escalates. Two further rows carry `(any forge)` in place of an operation and expand to one entry per operation whose forge call the condition prevented — `push`, `create_pr` and `merge` — each marked `forge_universal: true`. `rate_limited` and `forge_unavailable` are both `needs_caller`, so a throttle escalates rather than reaching the built-in `error` default that fails the flow, and both take the `retry_after` default need. They divide the transient conditions by repair rather than by cause: `rate_limited`'s wait is informed by the exhausted bucket's `resets_at` in `outputs.forge_budget`, `forge_unavailable`'s is not, and which of its three conditions occurred is reported in `outputs.forge_unavailable_condition` rather than in a reason of its own.",
    "entries": [
      { "token": "provision:ok", "operation": "provision", "reason": "ok", "class": "done", "default_need": null },
      { "token": "provision:unreachable", "operation": "provision", "reason": "unreachable", "class": "needs_caller", "default_need": "human_review" },
      { "token": "provision:store_unsupported", "operation": "provision", "reason": "store_unsupported", "class": "error", "default_need": null },
      { "token": "provision:failed", "operation": "provision", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "provision:unsupported", "operation": "provision", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "commit:ok", "operation": "commit", "reason": "ok", "class": "done", "default_need": null },
      { "token": "commit:nothing_to_commit", "operation": "commit", "reason": "nothing_to_commit", "class": "done", "default_need": null },
      { "token": "commit:worktree_moved", "operation": "commit", "reason": "worktree_moved", "class": "needs_caller", "default_need": "reread_then_retry" },
      { "token": "commit:identity_missing", "operation": "commit", "reason": "identity_missing", "class": "needs_caller", "default_need": "supply_identity" },
      { "token": "commit:blocked", "operation": "commit", "reason": "blocked", "class": "needs_caller", "default_need": "human_review", "universal": true },
      { "token": "commit:hook_unanswered", "operation": "commit", "reason": "hook_unanswered", "class": "error", "default_need": null, "universal": true },
      { "token": "commit:failed", "operation": "commit", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "commit:unsupported", "operation": "commit", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "integrate:ok", "operation": "integrate", "reason": "ok", "class": "done", "default_need": null },
      { "token": "integrate:up_to_date", "operation": "integrate", "reason": "up_to_date", "class": "done", "default_need": null },
      { "token": "integrate:merge_conflicts", "operation": "integrate", "reason": "merge_conflicts", "class": "needs_caller", "default_need": "resolve_conflicts" },
      { "token": "integrate:base_unresolved", "operation": "integrate", "reason": "base_unresolved", "class": "error", "default_need": null },
      { "token": "integrate:base_unavailable", "operation": "integrate", "reason": "base_unavailable", "class": "error", "default_need": null },
      { "token": "integrate:identity_missing", "operation": "integrate", "reason": "identity_missing", "class": "needs_caller", "default_need": "supply_identity" },
      { "token": "integrate:failed", "operation": "integrate", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "integrate:unsupported", "operation": "integrate", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "push:ok", "operation": "push", "reason": "ok", "class": "done", "default_need": null },
      { "token": "push:up_to_date", "operation": "push", "reason": "up_to_date", "class": "done", "default_need": null },
      { "token": "push:non_fast_forward", "operation": "push", "reason": "non_fast_forward", "class": "needs_caller", "default_need": "integrate_then_retry" },
      { "token": "push:pr_closed", "operation": "push", "reason": "pr_closed", "class": "needs_caller", "default_need": "human_review" },
      { "token": "push:rejected", "operation": "push", "reason": "rejected", "class": "error", "default_need": null },
      { "token": "push:rate_limited", "operation": "push", "reason": "rate_limited", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "push:forge_unavailable", "operation": "push", "reason": "forge_unavailable", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "push:blocked", "operation": "push", "reason": "blocked", "class": "needs_caller", "default_need": "human_review", "universal": true },
      { "token": "push:hook_unanswered", "operation": "push", "reason": "hook_unanswered", "class": "error", "default_need": null, "universal": true },
      { "token": "push:failed", "operation": "push", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "push:unsupported", "operation": "push", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "create_pr:created", "operation": "create_pr", "reason": "created", "class": "done", "default_need": null },
      { "token": "create_pr:updated", "operation": "create_pr", "reason": "updated", "class": "done", "default_need": null },
      { "token": "create_pr:base_mismatch", "operation": "create_pr", "reason": "base_mismatch", "class": "error", "default_need": null },
      { "token": "create_pr:conflict", "operation": "create_pr", "reason": "conflict", "class": "needs_caller", "default_need": "human_review" },
      { "token": "create_pr:rate_limited", "operation": "create_pr", "reason": "rate_limited", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "create_pr:forge_unavailable", "operation": "create_pr", "reason": "forge_unavailable", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "create_pr:blocked", "operation": "create_pr", "reason": "blocked", "class": "needs_caller", "default_need": "human_review", "universal": true },
      { "token": "create_pr:hook_unanswered", "operation": "create_pr", "reason": "hook_unanswered", "class": "error", "default_need": null, "universal": true },
      { "token": "create_pr:failed", "operation": "create_pr", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "create_pr:unsupported", "operation": "create_pr", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "merge:ok", "operation": "merge", "reason": "ok", "class": "done", "default_need": null },
      { "token": "merge:not_open", "operation": "merge", "reason": "not_open", "class": "needs_caller", "default_need": "human_review" },
      { "token": "merge:checks_pending", "operation": "merge", "reason": "checks_pending", "class": "needs_caller", "default_need": "await_checks" },
      { "token": "merge:checks_failed", "operation": "merge", "reason": "checks_failed", "class": "error", "default_need": null },
      { "token": "merge:conflict", "operation": "merge", "reason": "conflict", "class": "needs_caller", "default_need": "resolve_conflicts" },
      { "token": "merge:head_moved", "operation": "merge", "reason": "head_moved", "class": "needs_caller", "default_need": "reread_then_retry" },
      { "token": "merge:rejected", "operation": "merge", "reason": "rejected", "class": "error", "default_need": null },
      { "token": "merge:rate_limited", "operation": "merge", "reason": "rate_limited", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "merge:forge_unavailable", "operation": "merge", "reason": "forge_unavailable", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "merge:blocked", "operation": "merge", "reason": "blocked", "class": "needs_caller", "default_need": "human_review", "universal": true },
      { "token": "merge:hook_unanswered", "operation": "merge", "reason": "hook_unanswered", "class": "error", "default_need": null, "universal": true },
      { "token": "merge:failed", "operation": "merge", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "merge:unsupported", "operation": "merge", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "pull:ok", "operation": "pull", "reason": "ok", "class": "done", "default_need": null },
      { "token": "pull:conflict", "operation": "pull", "reason": "conflict", "class": "needs_caller", "default_need": "resolve_conflicts" },
      { "token": "pull:identity_missing", "operation": "pull", "reason": "identity_missing", "class": "needs_caller", "default_need": "supply_identity" },
      { "token": "pull:failed", "operation": "pull", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "pull:unsupported", "operation": "pull", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },
      { "token": "await_checks:ok", "operation": "await_checks", "reason": "ok", "class": "done", "default_need": null },
      { "token": "await_checks:checks_failed", "operation": "await_checks", "reason": "checks_failed", "class": "error", "default_need": null },
      { "token": "await_checks:still_pending", "operation": "await_checks", "reason": "still_pending", "class": "needs_caller", "default_need": "await_checks", "note": "The invocation's read allowance ended with the checks still pending. Stated over the allowance rather than over a supplied bound so it also covers the invocation that authorized no loop, whose allowance is one read (Sections 4.1, 8.1). Separate from `budget_floor` because the repairs differ: this one is met by waiting longer." },
      { "token": "await_checks:budget_floor", "operation": "await_checks", "reason": "budget_floor", "class": "needs_caller", "default_need": "retry_after", "note": "A supplied budget floor was reached with checks still pending; met by waiting for a bucket to refill rather than by raising the wait. Also reported where the observed snapshot cannot answer the floor — no snapshot, or no bucket of that name — an engine that cannot establish there is room to keep spending not continuing to spend. Where a read allowance and a floor are reached on the same read this is the reason, the floor judging the read just made while the allowance decides whether to read again (Sections 4.3, 8.1)." },
      { "token": "await_checks:no_checks", "operation": "await_checks", "reason": "no_checks", "class": "done", "default_need": null, "note": "The forge reports no required checks for the pull request, so there is nothing to wait for; ends the wait on the first read (Sections 4.1, 9.2). Class `done` because a wait for checks that do not exist is a benign no-op. A reason of its own rather than `ok` because a consumer that could not tell them apart could not see a merge gate stop existing — a required check dropped from branch protection turns every later merge into an unchecked one. Not `needs_caller`: requiring checks is a Way of Working the engine does not hold, so a repository that wants the anomaly surfaced binds this reason." },
      { "token": "await_checks:rate_limited", "operation": "await_checks", "reason": "rate_limited", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "await_checks:forge_unavailable", "operation": "await_checks", "reason": "forge_unavailable", "class": "needs_caller", "default_need": "retry_after", "forge_universal": true },
      { "token": "await_checks:failed", "operation": "await_checks", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "await_checks:unsupported", "operation": "await_checks", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "status:ok", "operation": "status", "reason": "ok", "class": "done", "default_need": null },
      { "token": "status:failed", "operation": "status", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "status:unsupported", "operation": "status", "reason": "unsupported", "class": "error", "default_need": null, "universal": true },

      { "token": "diff:ok", "operation": "diff", "reason": "ok", "class": "done", "default_need": null },
      { "token": "diff:base_unavailable", "operation": "diff", "reason": "base_unavailable", "class": "error", "default_need": null },
      { "token": "diff:failed", "operation": "diff", "reason": "failed", "class": "error", "default_need": null, "universal": true },
      { "token": "diff:unsupported", "operation": "diff", "reason": "unsupported", "class": "error", "default_need": null, "universal": true }
    ]
  },

  "hook_conditions": {
    "spec_refs": ["VCSX-SPEC.md Section 6.6 \"`[hooks]`\"", "VCSX-SPEC.md Section 8.2 \"Result Envelope\""],
    "note": "The three ways a unit the engine hands control to gives it no usable answer. Reported for a unit at a `before:*` position in `unanswered_gates` — a hook, or the `pr_to_squash` transform (Section 10.3) — and for a result-triggered hook in `unfinished_hooks`, spelled the same way in both, so one consumer branch reads both halves. The condition diagnoses; the reason routes (Section 4.3). `bound_elapsed` is shared with `forge_unavailable_conditions`, this group being the authority for its spelling.",
    "entries": [
      { "token": "bound_elapsed", "meaning": "The unit was still running when the bound elapsed, and was stopped." },
      { "token": "not_started", "meaning": "The engine could not start the unit." },
      { "token": "answer_unreadable", "meaning": "The unit answered, and the engine could not read the answer in the form it fixed." }
    ]
  },

  "forge_unavailable_conditions": {
    "spec_refs": ["VCSX-SPEC.md Section 4.3 \"Reason-Token Registry\"", "VCSX-SPEC.md Section 8.1 \"Entry Points and Arguments\"", "VCSX-SPEC.md Section 8.2 \"Result Envelope\"", "VCSX-SPEC.md Section 9 \"Plugin API\""],
    "note": "The three conditions behind a `forge_unavailable` result, reported in `outputs.forge_unavailable_condition` and absent for every other reason. The condition diagnoses; the reason routes (Section 4.3) — the same arrangement `hook_conditions` makes for its own three, and for the same reason. `bound_elapsed` is also a `hook_conditions` token: Section 6.6 is where the spelling is fixed and is the authority for it, and Section 9 reuses it deliberately, so that one event on two kinds of unit does not diagnose differently by which program the engine was waiting on. The duplication is carried rather than split so that this field's value space is one group a generator can read whole.",
    "entries": [
      { "token": "server_error", "meaning": "The forge answered that it is temporarily unable (Sections 4.3, 8.2)." },
      { "token": "bound_elapsed", "meaning": "The forge call was still outstanding when `network_bound_ms` elapsed, and was stopped (Sections 8.1, 9)." },
      { "token": "transport_failure", "meaning": "The forge did not answer: the call did not complete over its transport (Sections 4.3, 8.2)." }
    ]
  },

  "config_reasons": {
    "spec_refs": ["VCSX-SPEC.md Section 6.1 \"File Discovery and `vcsx.toml` Merge\"", "VCSX-SPEC.md Section 6.11 \"Validation\"", "VCSX-SPEC.md Section 5.6 \"Flow Bound and Termination\"", "VCSX-SPEC.md Section 9.3 \"Capability Descriptors\""],
    "note": "Carried in the envelope under the `usage_or_config` status, with null `op` and `class` — a refused policy has no operation result to classify. Part of the major-stable surface (Section 8.5). An engine MUST document any it adds. The first entry is a well-formedness failure and the rest are consistency failures: validation takes a document, so a policy that does not parse yields none for the other conditions to be judged against. Validation is judged from five inputs (Section 6.11): the policy document with `vcsx.toml` merged in, what the engine holds independently of the invocation (its own defaults), the consumer's selection and access configuration (the selected backends' descriptors among them), the actions the consumer can effect, and the repository units it bound. `policy_branch_is_target` turns on the consumer's configuration and the policy together rather than on any descriptor. `provision` is judged from those inputs less the policy document, since it is the operation that obtains the repository the document is in, so no reason here but `capability_unsupported` can be reported for it.",
    "entries": [
      { "token": "policy_source_unreadable", "meaning": "The source host-side policy is read from could not be read (Sections 6.1, 8.1). Names no cause beyond that: whether the branch is absent, the remote unreachable or the credential refused is not establishable from the far side of a transport, and a reason per cause would be a registry of the ways a network fails — the reading `provision:unreachable` already takes. One of four conditions that leave the engine without a usable policy; all four share a disposition and differ in reason, because what differs is the repair rather than the response." },
      { "token": "policy_not_found", "meaning": "The policy source was read and carries no `repo.policy.toml` at the discovered path (Section 6.1). One of the four unusable-policy conditions." },
      { "token": "malformed_policy", "meaning": "The policy is not well formed: a discovered `repo.policy.toml` (or a `vcsx.toml` merged into it) does not parse, a key's value does not satisfy the constraints its section states, an edge's action cannot be dispatched from the arguments it carries, or a declared hook names no unit to run. Reported for a well-formedness failure no other condition names." },
      { "token": "unknown_trigger", "meaning": "An edge's `on` is not a trigger the engine recognizes." },
      { "token": "unknown_action", "meaning": "An edge's `do` is not a known action." },
      { "token": "unknown_operation", "meaning": "A `run_op` names an operation the engine does not define." },
      { "token": "operation_not_dispatchable", "meaning": "A `run_op` names an operation that runs outside the action-policy machine (Section 4.1) — `load_policy` or `provision` today, and whichever operations a later MINOR release marks the same way. Not `unknown_operation` widened: the operation is one this specification defines, and the repairs differ — that reason says correct the spelling, this one says dispatch the operation from its own entry point (Section 8.1). Judged from the `policy_dispatchable` flag `operations` carries, which the specification fixes over the property rather than over the names it reaches." },
      { "token": "unknown_hook", "meaning": "A `run` names a hook the `[hooks]` table does not declare." },
      { "token": "duplicate_edge", "meaning": "A duplicate policy edge — two edges bound to one trigger, non-determinism. The trigger is the whole of an edge's key (Section 5.4); the `(from, on)` key below is `tracker.transitions`' own and is the consumer's." },
      { "token": "duplicate_transition", "meaning": "A duplicate `(from, on)` transition." },
      { "token": "duplicate_branch_section", "meaning": "Two `[[branch]]` sections with the same `match` (Section 6.10). Refused rather than merged in file order, on Section 5.4's reasoning one level up: a scheme in which two sections could both contribute an edge for one trigger reintroduces the non-determinism the one-edge-per-trigger rule refuses." },
      { "token": "position_cycle", "meaning": "A cycle of lifecycle positions, each position's `run_op` edge dispatching the operation the next position gates, so no operation on the cycle can run (Sections 4.1, 5.6). A position is matched exactly and binds at most one edge, so such a cycle reaches no operation on any traversal; a cycle through a typed operation result is not this condition and is bounded rather than refused." },
      { "token": "base_unresolvable", "meaning": "A `by_prefix` base resolution with no empty-prefix default, or a missing or malformed map." },
      { "token": "set_state_unbound", "meaning": "A `set_state`/transition binding without a consumer that can apply it." },
      { "token": "template_unbound", "meaning": "A `[messages.pr]` `body_source = \"template\"` with no template unit bound (Sections 5.2, 10.2). Judged from the repository units the consumer bound, which is why Section 6.11 names them among validation's inputs; a template is a repository unit rather than a Section 5.2 action, so `set_state_unbound`'s input does not reach it." },
      { "token": "transform_unbound", "meaning": "A `[messages.squash]` `transform` naming a unit the consumer bound nothing to (Sections 6.8, 10.3). Judged from the same input as `template_unbound` and refused for the same reason, with more of the flow behind it: a transform's first use is the `merge` a `land` reaches only once the pull request is open. A `[messages.squash]` naming no transform is not this condition — it names no unit, so nothing is unbound." },
      { "token": "capability_unsupported", "meaning": "A policy requiring a capability no selected backend declares (Section 9.3). Determinable because a statically declared capability follows from the consumer's selection alone; a descriptor field only the checkout answers is not, and keeps Section 9.3's first-use disposition." },
      { "token": "policy_branch_is_target", "meaning": "The `policy_branch` names the same branch as the resolved base (Sections 6.4, 8.1). Judged from the consumer's configuration and the policy together and from no checkout, which places it here rather than among the preconditions. A trusted revision that is also the pull-request target is one the work being landed can rewrite; refusing at validation keeps the refusal ahead of `commit`, so nothing is published by an invocation that cannot open a pull request." },
      { "token": "version_floor_unmet", "meaning": "A `version_floor` above the running engine version." },
      { "token": "branch_section_selector_key", "meaning": "A `[[branch]]` section carries `[base]` or `[scope]`, either of which supplies the value that selects the section (Sections 6.4, 6.10). Base resolution runs before section selection and reads no section, so a section naming either would select itself — directly through `[base]`, or one step longer through `[scope] branch_pattern`, which a `by_prefix` resolution reads. Refused rather than resolved as a fixpoint so that validity stays answerable by looking at the document; a repository varying its base per track states it at the top level with `resolve = \"by_prefix\"`." }
    ]
  },

  "precondition_reasons": {
    "spec_refs": ["VCSX-SPEC.md Section 8.6 \"Invocation Preconditions\""],
    "note": "Carried in the envelope under the `usage_or_config` status, with null `op` and `class` — no operation ran, so there is no result to classify. Distinct from `config_reasons` in one direction only: a configuration error is judged from the five inputs Section 6.11 names, none of which reads the checkout. The converse is not claimed — a precondition MAY need the checkout and MAY be judged from the invocation's arguments alone, as `arguments_unreadable`, `local_vcs_missing`, `forge_coordinate_missing`, `git_access_missing`, `forge_access_missing`, `store_location_missing` and `await_bound_missing` are. Where both sides are checkout-free, what separates them is the artifact at fault: a configuration error is repaired by editing a document, a precondition failure by changing the invocation. Validation precedes precondition establishment, so where both hold the configuration reason is reported — with three exceptions established before validation: `arguments_unreadable`, because an engine that cannot decode its arguments cannot locate the policy it would validate; `local_vcs_missing`, because the selection is what fixes whose descriptor validation reads; and the argument that says where the policy document is read from — `policy_branch_missing` under `policy_source = \"policy_branch\"` and `base_branch_missing` under `target_branch`, exactly one of which applies to any invocation. `provision` establishes only the reasons judged from the invocation's arguments and none that read a checkout, being the operation that produces one (Section 8.6). Part of the major-stable surface (Section 8.5). An engine MUST document any it adds.",
    "entries": [
      { "token": "arguments_unreadable", "meaning": "The invocation's arguments could not be decoded in the encoding the engine published (Section 8.1). Established before validation rather than after it." },
      { "token": "local_vcs_missing", "meaning": "No `local_vcs` was supplied, so no VCS backend is selected (Section 8.1). Established before validation, and after `arguments_unreadable`: the selection is what fixes whose descriptor validation reads, so a `capability_unsupported` presupposes a backend this argument names." },
      { "token": "policy_branch_missing", "meaning": "No `policy_branch` was supplied, so the policy cannot be located (Section 8.1). Established before validation, being the mode-dependent third of the three that are — under `target_branch` that place is `base_branch_missing`'s (Section 8.6): the policy document is the first of Section 6.11's five inputs, and this argument is what says where to read it from, so there is nothing to validate until it is known." },
      { "token": "forge_coordinate_missing", "meaning": "A forge is configured and no forge repository coordinate was supplied (Section 8.1). A coordinate the backend cannot use is that backend's own `failed` at first use instead, because the engine holds the coordinate opaque and judges no shape." },
      { "token": "git_access_missing", "meaning": "An entry that can reach a remote was invoked and no `git_access` was supplied (Section 8.1). A `git_access` the backend cannot use is that backend's own `failed` at first use instead, because the engine holds the parameter opaque and judges no shape." },
      { "token": "forge_access_missing", "meaning": "A forge is configured and no `forge_access` was supplied (Section 8.1). A `forge_access` the backend cannot use is that backend's own `failed` at first use instead, on the same reading." },
      { "token": "store_location_missing", "meaning": "`provision` was invoked and no `store_location` was supplied (Section 8.1). The argument carries no meaning for any other entry, so its absence is refused only here. A `store_location` the backend cannot use is that backend's own `failed` at first use instead, on the same reading as the access parameters." },
      { "token": "base_branch_missing", "meaning": "Scoped by the policy source (Section 8.1). Under `policy_source = \"policy_branch\"`: an entry that needs a base — `integrate`, `create_pr`, and a front-end sequence that dispatches one — was invoked and none of the three sources supplied a base branch: the invocation's `base_branch`, the consumer configuration's, or `[base] branch` in the policy (Section 6.4); `commit`, `push`, `pull`, `merge`, `land` and `provision` need none and run without one. Under `target_branch`: any entry but `provision` was invoked and neither the invocation nor the consumer configuration supplied one, `[base]` contributing nothing under that mode because it sits in the document the base locates — and established before validation there, being the argument that says where the policy is read from. `provision` is exempt under either mode, performing no policy read to locate. The one precondition judged partly from the policy document, and only under the default mode, and still a precondition rather than a configuration error, because a document omitting an OPTIONAL key is well formed and what is absent is a value the invocation was free to supply." },
      { "token": "base_branch_not_permitted", "meaning": "A supplied `base_branch` is outside the consumer configuration's `base_branch_allowed` (Section 8.1). Judged wherever a base was supplied, whatever the entry, because the bound is about what an invocation may name rather than about what an entry needs — the same shape as a malformed commit identity." },
      { "token": "no_current_branch", "meaning": "The work branch is the checkout's current branch (no `branch_pattern` configured) and the checkout has none." },
      { "token": "work_branch_invalid", "meaning": "The derived work branch name is not a legal branch name for the VCS backend." },
      { "token": "identity_invalid", "meaning": "The caller-supplied commit identity is absent where the entry requires one — `commit`, `integrate`, `pull`, and a front-end sequence that dispatches one — or is malformed as the VCS backend judges it, which is judged whatever the entry. An identity absent at an entry the requirement does not cover is the operation's `identity_missing` at the dispatch instead (Section 4.3)." },
      { "token": "checkout_unreadable", "meaning": "A VCS backend capability the engine consults before the first dispatch could not answer — the checkout could not be read. Distinct from `no_current_branch` and `work_branch_invalid`, which are answers the backend gave; this is the absence of one (Sections 8.6, 9)." },
      { "token": "resume_unusable", "meaning": "A supplied `resume` the engine cannot establish as its own and current — issued under a different policy, against a different repository, by a different major version, or by an entry point other than the one the resuming invocation names (Sections 5.5, 8.1). Judged wherever a `resume` was supplied, whatever the entry, from the invocation's arguments together with the policy the engine validated and its own version; the fourth condition is judged from the arguments alone, which is what makes the reason decidable at the two entry points that run no policy — `provision` and `load_policy` issue no token, so a token supplied to either fails on the entry point and on nothing else; an absent `resume` is no failure and reaches no row here. A precondition rather than a configuration error because the artifact at fault is the invocation: the policy is well formed and what is wrong is a value the caller carried forward past the point it described anything. Refusing costs a re-invocation from the entry point, where accepting a stale one runs an operation the policy no longer routes." },
      { "token": "policy_pin_unmatched", "meaning": "A supplied `policy_pin` the engine cannot establish as naming the policy surface it validated (Sections 4.1, 8.1). Judged wherever a `policy_pin` was supplied, whatever the entry. A reason of its own rather than `resume_unusable` because the repairs differ: that says re-invoke from the entry point, this says re-read the policy and decide whether this is still one unit of work. An absent pin is no failure and reaches no row here, and a resumed invocation is not required to supply one, its token carrying the same judgement. Its input is the surface the invocation validated, as `resume_unusable`'s policy condition is; the absence of one is as determinate as a mismatch, so a pin supplied to `provision` — which validates no surface — is always refused." },
      { "token": "await_bound_missing", "meaning": "An await parameter that can only end a wait — `await_interval_ms`, `await_budget_floor` — was supplied with neither `await_bound_ms` nor `await_max_reads`, so nothing authorized a second read (Section 8.1). Judged wherever an await parameter was supplied, whatever the entry, because what is wrong is the combination the invocation named rather than an argument the entry required. Not one of the rows naming a missing argument: nothing here is required and absent, and an invocation supplying no await parameter at all is refused by nothing and makes a single read." }
    ]
  },

  "invocation_statuses": {
    "spec_refs": ["VCSX-SPEC.md Sections 8.2 \"Result Envelope\", 8.3 \"Exit Codes\""],
    "note": "Part of the major-stable surface (Section 8.5). The first three are the overall proto class of a run that executed the policy; `usage_or_config` covers a run in which it did not — a refused policy (Section 6.11) or an unestablished precondition (Section 8.6).",
    "entries": [
      { "token": "ok", "exit_code": 0 },
      { "token": "needs_caller", "exit_code": 10 },
      { "token": "error", "exit_code": 20 },
      { "token": "usage_or_config", "exit_code": 2 }
    ]
  },

  "trigger_kinds": {
    "spec_refs": ["VCSX-SPEC.md Sections 5.1 \"Triggers\", 5.3 \"Matching Algorithm and the `#class` Fallback\""],
    "note": "Two kinds, both produced by the engine itself — a position it entered, a result an operation it ran returned — so a trigger's producer and its matcher sit inside one invocation. An event the consumer observes is not a trigger: it selects which entry point the consumer invokes, which is what `[driver]` is for (Sections 5.1, 6.9). The tables a consumer reads — `tracker.transitions`, `[tasks]`, `[driver]` — are carried in the merged surface and validated for determinism without the executor matching their `on`; their vocabularies are the consumer's and are published by the consumer (for Symphony, `conformance/vocabulary.json` `transition_triggers`).",
    "entries": [
      { "kind": "lifecycle_position", "form": "before:<op>", "class_fallback": false },
      { "kind": "typed_result", "form": "<op>:<reason>", "class_fallback": true, "ladder": ["op:reason", "op:#class", "#class", "built-in default"] }
    ]
  },

  "actions": {
    "spec_refs": ["VCSX-SPEC.md Section 5.2 \"Actions\""],
    "note": "`effected_by` records who performs the action: the engine's own, or the consumer's, because it touches a system outside the VCS/forge domain.",
    "entries": [
      { "token": "run_op", "effected_by": "engine", "args": ["op", "args?"] },
      { "token": "run", "effected_by": "engine", "args": ["hook", "context"] },
      { "token": "escalate", "effected_by": "engine", "args": ["reason?"], "note": "Abstract; the front-end binds the resolver (Section 5.5). `reason` is OPTIONAL: an edge naming none raises the trigger's default need (Section 4.3), and `human_review` where the trigger carries none." },
      { "token": "create_task", "effected_by": "consumer", "args": ["spec"] },
      { "token": "set_state", "effected_by": "consumer", "args": ["target"] },
      { "token": "notify", "effected_by": "consumer", "args": ["channel", "payload"] },
      { "token": "park", "effected_by": "engine", "args": [] },
      { "token": "fail", "effected_by": "engine", "args": ["reason?"], "note": "`reason` is OPTIONAL and is a repository-authored token rather than one of this file's registries; it is reported in `outputs.failed_by_policy` and in `message`, never in the envelope's `reason` field." }
    ]
  },

  "needs": {
    "spec_refs": ["VCSX-SPEC.md Section 8.4 \"Escalation Payload\""],
    "note": "Part of the public contract: documented and stable within a MAJOR. The set below is the specification's named vocabulary; an engine documents any it adds. `raised_by` records what raises the need — the action that raises it, or `executor` for one no policy can write — and `resolvable` whether a front-end binds a resolver to it. `retryable` is fixed per need and means that re-invoking the same entry point with the same arguments, after a delay and with no further action by the caller, MAY succeed. It follows from a reason's `default_need` rather than being carried on the reason, so the two cannot disagree, and it is part of the major-stable surface (Section 8.5) — carried in the escalation rather than derived by a consumer, so a need added in a MINOR release is absorbable.",
    "entries": [
      { "token": "integrate_then_retry", "raised_by": "escalate", "resolvable": true, "retryable": false, "note": "Not retryable: an `integrate` must run first, so re-invoking unchanged reproduces the same `push:non_fast_forward`." },
      { "token": "reread_then_retry", "raised_by": "escalate", "resolvable": true, "retryable": true, "note": "Met by reading the moved state again and retrying, which is the re-entry a resume performs (Section 5.5), rather than by supplying anything. The default need of `commit:worktree_moved` and `merge:head_moved`. Retryable, the re-read being what a re-invocation does." },
      { "token": "resolve_conflicts", "raised_by": "escalate", "resolvable": true, "retryable": false },
      { "token": "supply_identity", "raised_by": "escalate", "resolvable": true, "retryable": false },
      { "token": "await_checks", "raised_by": "escalate", "resolvable": true, "retryable": true },
      { "token": "retry_after", "raised_by": "escalate", "resolvable": true, "retryable": true, "note": "The need a transient forge condition raises (Section 4.3). It names a wait whose length is not carried in the escalation: where the forge reported one, the exhausted bucket's `resets_at` is in `outputs.forge_budget`, and duplicating it would give a consumer two places to read one figure from and two ways for them to disagree." },
      { "token": "human_review", "raised_by": "escalate", "resolvable": true, "retryable": false },
      { "token": "intervention", "raised_by": "park", "resolvable": false, "retryable": false, "note": "Names a hold rather than a request: a front-end MUST NOT bind a resolver to it and MUST NOT resume the flow on it." },
      { "token": "flow_exhausted", "raised_by": "executor", "resolvable": false, "retryable": false, "note": "Names a hold rather than a request, imposed by the executor when a flow reaches a bound (Section 5.6) rather than asked for by the policy: a front-end MUST NOT bind a resolver to it and MUST NOT resume the flow on it." }
    ]
  },

  "envelope_fields": {
    "spec_refs": ["VCSX-SPEC.md Section 8.2 \"Result Envelope\""],
    "note": "Part of the major-stable surface. A consumer MAY add fields but SHOULD NOT break these within a MAJOR. `nullable` records whether the specification admits a null, so a generated envelope type takes its optionality from here rather than from Section 8.2's example.",
    "entries": [
      { "token": "vcsx_version", "nullable": false },
      { "token": "entry", "nullable": true, "note": "Null exactly where no Section 8.1 entry point was read: the `usage_or_config` status carrying the `arguments_unreadable` reason, and nowhere else." },
      { "token": "status", "nullable": false },
      { "token": "op", "nullable": true, "note": "Null where the run has no decisive operation result, and under `usage_or_config`." },
      { "token": "reason", "nullable": true, "note": "Null where the run has no decisive operation result; carries a configuration or precondition reason under `usage_or_config`." },
      { "token": "class", "nullable": true, "note": "Null where the run has no decisive operation result, and under `usage_or_config`." },
      { "token": "escalation", "nullable": true, "note": "Present exactly when `status == \"needs_caller\"` (Section 8.4)." },
      { "token": "outputs", "nullable": false },
      { "token": "message", "nullable": false, "note": "Human-readable prose. Nothing parses it: every fact a consumer branches on has a field or a token of its own." }
    ]
  },

  "output_keys": {
    "spec_refs": ["VCSX-SPEC.md Section 8.2 \"Result Envelope\""],
    "note": "The keys Section 8.2 fixes inside `outputs`. Each is absent or empty where the condition it reports did not occur; the rest of `outputs` is entry-specific and is not a shared vocabulary.",
    "entries": [
      { "token": "unperformed_intents", "fields": [{ "name": "action" }, { "name": "arguments" }], "meaning": "The consumer-effected intents the engine emitted and no consumer performed (Section 5.2). `action` carries no `values_from`: its value space is the `effected_by: \"consumer\"` subset of `actions` rather than that group, and a subset is not a link this registry can state without fixing a property the specification does not." },
      { "token": "unfinished_hooks", "fields": [{ "name": "hook" }, { "name": "trigger" }, { "name": "condition", "values_from": "hook_conditions" }], "meaning": "The result-triggered hooks that gave the engine no usable answer (Section 6.6)." },
      { "token": "unanswered_gates", "fields": [{ "name": "hook" }, { "name": "position", "values_from": "lifecycle_positions" }, { "name": "condition", "values_from": "hook_conditions" }, { "name": "detail" }], "meaning": "The `before:*` units that gave the engine no usable answer, whose operations reported `hook_unanswered` (Sections 4.3, 6.6). The key is named for the gate that is almost always the unit at a position; the `pr_to_squash` transform is reported here too (Section 10.3). `detail` is Implementation-defined. `position` carries a `values_from` link: the lifecycle positions are the set Section 4.1 fixes and no engine adds to, so the field's value space is exactly that group (Sections 4.1, 5.1, 8.5). An array, because one traversal can pass more than one unanswered unit." },
      { "token": "failed_by_policy", "fields": [{ "name": "trigger" }, { "name": "reason" }], "meaning": "Present where the policy ended the flow with `fail` (Section 5.2). `reason` is the repository-authored token the edge wrote and is absent where the edge wrote none; it belongs to no registry in this file." },
      { "token": "forge_budget", "fields": [{ "name": "buckets" }, { "name": "observed_at" }], "meaning": "The most recent budget snapshot a forge capability observed during the invocation (Sections 8.2, 9.2), reported whether or not any limit was reached. Each bucket carries `limit`, `remaining` and an OPTIONAL `resets_at` under the name the forge used; identity is opaque and the counts are in the bucket's own unit, so nothing here is normalized, summed or compared. Absent both where the invocation reached no forge capability and where it reached one and the forge reported no budget — one spelling for two events, because the consumer learned nothing new in either." },
      { "token": "forge_unavailable_condition", "values_from": "forge_unavailable_conditions", "meaning": "The condition behind a `forge_unavailable` result (Sections 4.3, 8.2), named by a `forge_unavailable_conditions` token. Absent for every other reason. A scalar rather than an array of records, so the link is carried at entry level. The reason routes and the condition diagnoses, as `hook_unanswered` and `unanswered_gates` already divide the work." },
      { "token": "pr_state_unchanged", "meaning": "Present on a `status` whose invocation supplied a `pr_state_validator` the forge reported unmoved (Sections 4.1, 8.1, 9.2). Distinct from an absent pull request and from `pr_state_unavailable`: the read established that the caller's copy is current." },
      { "token": "pr_state_unavailable", "meaning": "Present on a `status` whose configured forge could not be asked for the pull-request state (Sections 4.1, 9.2): the read established nothing, which is neither an absent pull request nor an unchanged one." },
      { "token": "pr_state_throttled", "meaning": "Present on a `status` whose forge refused the pull-request read because a budget was exhausted (Sections 4.1, 9.2). The fourth distinguishable pull-request condition, beside an unchanged read, an unavailable one and a reported state. Spelled apart from `pr_state_unavailable` for the reason `rate_limited` and `forge_unavailable` are spelled apart one layer up: the repair here is informed, the exhausted bucket and its `resets_at` being in `forge_budget`, and there uninformed. `status` still completes — the operation reports rather than acts, so a refused forge call costs it one field rather than the run, which is why this is an output and not a reason (Section 4.3)." },
      { "token": "resume_token", "meaning": "An opaque token naming the point that raised the need, the root trigger the chain that point belongs to descends from, and the flow bound already spent, which a later invocation supplies as `resume` (Sections 5.5, 5.6, 8.1). Present exactly where the invocation ended at `needs_caller` with a resolvable need; absent for the two holds, `intervention` and `flow_exhausted`, so the prohibition on resuming either is readable from the envelope rather than only from the policy that produced it. It carries the point, the root trigger and the count, and nothing a lifecycle position established — an `expected_worktree` or `expected_head` carried across would hand an operation state no position had inspected since. Each of the three parts is fixed-width, and the trigger is carried by its registry token rather than by an ordinal into a generated enumeration (Section 5.5)." },
      { "token": "policy_pin", "meaning": "An opaque handle naming the policy surface the invocation validated, which a later invocation supplies as `policy_pin` to claim the two are one unit of work (Sections 4.1, 8.1, 8.6). Present where a surface was validated and absent where none was — which leaves exactly one entry point where it is always absent, `provision`, the one that runs where no policy could be read, together with any run refused before validation. Not scoped to `load_policy`: every other entry reads and validates the document in order to run it, so the pin names a surface the invocation had already established. The handle form is a necessity rather than a choice — a value form would oblige the specification to fix a canonicalization of the effective surface, over a document Section 6.1 places nowhere — and no two engines compare pins, so a consumer that inspects one has a bug." }
    ]
  },

  "escalation_fields": {
    "spec_refs": ["VCSX-SPEC.md Section 8.4 \"Escalation Payload\""],
    "entries": ["need", "retryable", "op", "detail"]
  },

  "exit_codes": {
    "spec_refs": ["VCSX-SPEC.md Section 8.3 \"Exit Codes\""],
    "note": "Subprocess encoding. Part of the major-stable surface. The JSON envelope is emitted for each of the four status-bearing codes, and on every path that produces a result stdout carries exactly one JSON object and nothing else. Any code other than the four status-bearing ones means the invocation produced no result, as `1` does.",
    "entries": [
      { "code": 0, "status": "ok" },
      { "code": 10, "status": "needs_caller" },
      { "code": 20, "status": "error" },
      { "code": 2, "status": "usage_or_config", "note": "The policy did not run (Sections 6.11, 8.6)." },
      { "code": 1, "status": null, "note": "The invocation produced no Section 8.2 result: stdout carries nothing and the diagnostic goes to stderr. Not an invocation status." }
    ]
  },

  "entry_points": {
    "spec_refs": ["VCSX-SPEC.md Sections 7 \"Front-Ends\", 8.1 \"Entry Points and Arguments\""],
    "entries": [
      { "token": "ship", "kind": "front_end_sequence" },
      { "token": "land", "kind": "front_end_sequence" },
      { "token": "load_policy", "kind": "operation" },
      { "token": "provision", "kind": "operation" },
      { "token": "status", "kind": "operation" },
      { "token": "diff", "kind": "operation" },
      { "token": "commit", "kind": "operation" },
      { "token": "integrate", "kind": "operation" },
      { "token": "push", "kind": "operation" },
      { "token": "create_pr", "kind": "operation" },
      { "token": "merge", "kind": "operation" },
      { "token": "pull", "kind": "operation" },
      { "token": "await_checks", "kind": "operation" }
    ]
  },

  "arguments": {
    "spec_refs": ["VCSX-SPEC.md Section 8.1 \"Entry Points and Arguments\"", "VCSX-SPEC.md Section 8.6 \"Invocation Preconditions\""],
    "note": "The arguments VCSX-SPEC.md Section 8.1 names, closed there: membership is exactly the arguments that section documents in its own bullet form, and an argument added to the section without an entry here — or an entry here the section does not name — is drift. `optional` carries the section's own OPTIONAL marker, or the presence of a stated default where the section marks none; `consumer_configuration` whether the engine MAY read the value from the consumer configuration, false for the four the section excepts — the two read validators, `resume` and `policy_pin` — each of which is a value a previous invocation returned and a configured copy of which would be stale by construction (Section 1.3). `required` is not a boolean and not a list of entry points, because Section 8.1 states requiredness in three shapes: at every entry point (`every_entry`, `local_vcs`), at named entry points (`at_entries`, `store_location` for `provision`), and under a condition the section states rather than an entry list (`conditional`, `git_access`, `forge_access`, `policy_branch`); `none` marks an argument no entry requires, and null an argument whose requiredness the section does not state. Section 8.1 also names four things it fixes no token for — the forge repository coordinate, the identity the work branch is derived from, the commit identity, and the execution context — so this group is the section's named arguments and not the whole of its argument surface; a precondition reason names the first of them (`forge_coordinate_missing`) and an argument does not. No field marks which arguments a resumed invocation consults: that follows from where the flow reads an argument rather than from a class of arguments, a resumed `land` consulting no `await_first` because the await branch runs ahead of the point a resume re-enters, and a resumed `ship` consulting `message` because the commit loop reads it at every turn (Sections 8.1, 12.2, 12.3).",
    "entries": [
      { "token": "local_vcs", "optional": false, "consumer_configuration": true, "required": { "kind": "every_entry" }, "note": "The VCS backend the plugin layer loads, and the checkout mode for a checkout the engine creates. Its absence is `local_vcs_missing`, established before validation, the selection being what fixes whose descriptor validation reads (Sections 3.3, 8.6)." },
      { "token": "forge", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The forge backend the plugin layer loads. \"A forge is configured\" throughout VCSX-SPEC.md means this argument is present." },
      { "token": "remote", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The remote the version-control operations that touch one act against. Unset, the backend's default remote for the checkout mode, which is Implementation-defined." },
      { "token": "base_branch", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The base for this invocation, winning over the consumer configuration's and over `[base] branch` (Section 6.4). Whether an entry requires a base at all is scoped by the policy source rather than by this argument (`base_branch_missing`, Section 8.6); one outside `base_branch_allowed` is `base_branch_not_permitted` wherever it is supplied." },
      { "token": "base_branch_allowed", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The set of bases an invocation may name. It belongs to the consumer configuration rather than to a single invocation, so the party bounding the choice is not the party making it; the `policy_branch` is excluded whatever this lists and whether or not it is configured." },
      { "token": "policy_source", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "`policy_branch` or `target_branch`, defaulting to the former. A named mode rather than a flag because the Section 11 trust properties are conditional on it." },
      { "token": "policy_branch", "optional": false, "consumer_configuration": true, "required": { "kind": "conditional", "condition": "policy_source = \"policy_branch\"", "spec_ref": "VCSX-SPEC.md Section 8.1 \"Entry Points and Arguments\"" }, "note": "The revision the host-side parts of `repo.policy.toml` are read from under the default mode. Its absence is then `policy_branch_missing`, established before validation. It resolves to the copy belonging to the resolved `remote` and never to a local branch of the same name." },
      { "token": "store_location", "optional": false, "consumer_configuration": true, "required": { "kind": "at_entries", "entries": ["provision"] }, "note": "Where the fetched copy of the repository is maintained. It carries no meaning for any other entry, so its absence is refused only at `provision` (`store_location_missing`)." },
      { "token": "tree_location", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "Where the working tree is derived from the store. Absent, `provision` maintains the store and derives no tree." },
      { "token": "git_access", "optional": false, "consumer_configuration": true, "required": { "kind": "conditional", "condition": "an entry that can reach a remote — `provision`, `integrate`, `push`, `pull`, and a front-end sequence that dispatches one", "spec_ref": "VCSX-SPEC.md Section 8.6 \"Invocation Preconditions\"" }, "note": "Where the version-control operations reach the remote. An entry outside that set reaching `integrate`, `push` or `pull` through a `run_op` edge reports that operation's own `failed` instead; no edge may name `provision` (Sections 4.1, 6.11)." },
      { "token": "forge_access", "optional": false, "consumer_configuration": true, "required": { "kind": "conditional", "condition": "a forge is configured", "spec_ref": "VCSX-SPEC.md Section 8.6 \"Invocation Preconditions\"" }, "note": "Where the forge operations reach the code host. Every Section 9.2 capability acts against it." },
      { "token": "await_bound_ms", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The overall wall-clock bound on the wait. One of the two that authorize a second read." },
      { "token": "await_max_reads", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The greatest number of reads `await_checks` makes. The other that authorizes a second read." },
      { "token": "await_interval_ms", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The least time between two reads. It paces an allowance already authorized and cannot create one, so supplied with neither of the two above it is `await_bound_missing` (Section 8.6)." },
      { "token": "await_budget_floor", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "A bucket name and a `min_remaining` compared against the snapshot each read observes. It can only end a wait early, so it is refused on the same terms as `await_interval_ms`." },
      { "token": "await_first", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "Supplied to `land`, it dispatches `await_checks` before the `merge` the sequence already runs (Sections 7.2, 12.3). It carries no meaning at any other entry point, and it is not one of the four await parameters: those bound a wait the operation makes, and this decides whether the sequence dispatches the operation at all. A resumed `land` does not consult it, the await branch running ahead of the point a resume re-enters." },
      { "token": "network_bound_ms", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The bound applied to each network call an invocation makes — one call, not the sum of an operation's calls. Its default is Implementation-defined, and an engine MUST admit a configured value of at least 600 seconds." },
      { "token": "pr_state_validator", "optional": true, "consumer_configuration": false, "required": { "kind": "none" }, "note": "The validator a previous invocation's `pr_state` read returned in `outputs`. Presented on the `status` read; the engine MUST NOT present it to a capability that did not issue it. Not readable from the consumer configuration: it changes with each read." },
      { "token": "checks_state_validator", "optional": true, "consumer_configuration": false, "required": { "kind": "none" }, "note": "The validator a previous invocation's `checks_state` read returned. Presented on the first `await_checks` read, the engine carrying it forward from each read to the next within one wait. Not readable from the consumer configuration, on the same ground." },
      { "token": "forge_parameters", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "A per-backend parameter set the engine carries to the selected forge backend uninterpreted. The keys are Implementation-defined per backend and a backend MUST document those it reads." },
      { "token": "effectable_actions", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "Which of the consumer-effected actions this consumer can perform, defaulting to empty. The input `set_state_unbound` turns on (Sections 5.2, 6.11)." },
      { "token": "bound_units", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The repository unit names the consumer bound, defaulting to empty. The input `template_unbound` and `transform_unbound` turn on (Sections 6.11, 10.2, 10.3)." },
      { "token": "resume", "optional": true, "consumer_configuration": false, "required": { "kind": "none" }, "note": "The `resume_token` a previous invocation returned. Supplied, the invocation re-enters the point that raised the need rather than beginning at its entry point, continues the flow from there rather than ending at the re-entered result (Section 5.5), and the flow bound continues from the count the token carries. Established against four conditions — the policy, the repository, the major version, and the entry point that issued it — whose failure is `resume_unusable` (Sections 5.5, 8.6). Not readable from the consumer configuration." },
      { "token": "policy_pin", "optional": true, "consumer_configuration": false, "required": { "kind": "none" }, "note": "The `policy_pin` a previous invocation returned, naming the policy surface a unit of work began under. Supplied, the invocation is refused where the surface it validated is not the one the pin names (`policy_pin_unmatched`, Section 8.6). Not readable from the consumer configuration." },
      { "token": "git_credential", "optional": null, "consumer_configuration": true, "required": null, "note": "The credential presented at `git_access`. Section 8.1 marks it neither OPTIONAL nor REQUIRED and no precondition names its absence, so `optional` and `required` are null rather than guessed; a credential a backend cannot use is that backend's own failure at first use. It reaches the plugins for the duration of one invocation and the engine persists none beyond it (Section 1.3)." },
      { "token": "forge_credential", "optional": true, "consumer_configuration": true, "required": { "kind": "none" }, "note": "The credential presented at `forge_access`, defaulting to `git_credential`. Supplied with the access parameter it is used against, so the credential and the endpoint it reaches are one decision made by one party (Section 11)." }
    ]
  },

  "execution_contexts": {
    "spec_refs": ["VCSX-SPEC.md Section 3.2 \"Execution Contexts (Trust)\""],
    "note": "Not declared in `repo.policy.toml`. A `[policy]` edge's context and a `[hooks.engine.<name>]` hook's context are both fixed by the artifact each was declared in — one in `repo.policy.toml` is `host_side`, one in the consumer's in-sandbox artifact is `in_sandbox` — and the consumer tags each while assembling the one merged surface the engine reads (Sections 3.2, 6.5, 6.6). No context is derived from the operation a `run_op` edge names. A `context` key on an edge is ignored under Section 6.1's unknown-key rule rather than refused.",
    "entries": ["host_side", "in_sandbox"]
  },

  "checkout_modes": {
    "spec_refs": ["VCSX-SPEC.md Section 3.3 \"Checkout Modes\""],
    "note": "Detection mechanism is Implementation-defined and MUST be documented. `jj` includes a colocated checkout and a secondary workspace with no colocated git storage.",
    "entries": ["git", "jj"]
  },

  "repo_policy_sections": {
    "spec_refs": ["VCSX-SPEC.md Section 6 \"`repo.policy.toml` Schema\""],
    "note": "`vcsx.toml` is merged in per Section 6.1. Entries are in section order. The engine's hooks are `[hooks.engine]`; a consumer MAY carry its own under a disjoint prefix within `hooks`, which this specification does not define and which therefore has no entry here.",
    "entries": [
      { "token": "[requires]", "spec_ref": "Section 6.2" },
      { "token": "[scope]", "spec_ref": "Section 6.3" },
      { "token": "[base]", "spec_ref": "Section 6.4" },
      { "token": "[policy]", "spec_ref": "Section 6.5" },
      { "token": "[hooks.engine]", "spec_ref": "Section 6.6" },
      { "token": "tracker.transitions", "spec_ref": "Section 6.7" },
      { "token": "[messages]", "spec_ref": "Section 6.8" },
      { "token": "[tasks]", "spec_ref": "Section 6.9" },
      { "token": "[driver]", "spec_ref": "Section 6.9" },
      { "token": "[[branch]]", "spec_ref": "Section 6.10" }
    ]
  },

  "message_formulation": {
    "spec_refs": ["VCSX-SPEC.md Section 10 \"Message Formulation\"", "VCSX-CONTRACT.md Section 9 \"Message Formulation\""],
    "note": "The engine supplies the seams; the repository supplies the format. Scan profile names such as `strict` and `relaxed` are repository-bound, not engine tokens, and no configuration key binds one: a scan is declared as a hook and reached through a `[policy]` edge at a lifecycle position (Sections 6.5, 6.8, 10.4).",
    "entries": [
      { "token": "scan-content", "kind": "hook", "bound_by": "a `[policy]` edge at a lifecycle position", "positions": ["before:commit", "before:create_pr"], "spec_ref": "Section 10.4" },
      { "token": "pr_to_squash", "kind": "transform", "bound_by": "`[messages.squash]` `transform`, whose unit the consumer binds", "position": "before:merge", "spec_ref": "Section 10.3" },
      { "token": "body_source", "kind": "config_key", "values": ["auto", "agent", "template"], "spec_ref": "Section 10.2" },
      { "token": "strategy", "kind": "config_key", "values": ["squash", "merge", "rebase"], "default": "merge", "spec_ref": "Section 6.8" }
    ]
  },

  "task_model": {
    "spec_refs": ["VCSX-SPEC.md Section 7.3 \"The Embedded-Driver Contract\"", "VCSX-CONTRACT.md Section 8 \"Task Model and Broker Task Verbs\""],
    "note": "The task model is the consumer's; the engine consumes only the resulting task-state events. The broker task verbs are exposed by the consumer's broker CLI, not by the engine.",
    "fields": ["id", "description", "status", "assignee", "parent", "tracker_link"],
    "status_values": ["open", "closed", "blocked"],
    "assignee_values": ["agent", "human"],
    "broker_verbs": ["add", "split", "close", "need-help", "update"]
  }
}
